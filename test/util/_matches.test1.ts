// import _matches from 'lodash/matches';
// import _filter from 'lodash/filter';
// import { matches } from '../../src/util';
// import { filter } from '../../src/collection'

// const objects = [
// 	{ 'a': 1, 'b': 2, 'c': 3 },
// 	{ 'a': 4, 'b': 5, 'c': 6 }
// ];

// describe('Util', () => {
// 	describe('matches', () => {
// 		it("filter(objects, matches({ 'a': 4, 'c': 6 }))", () => {
// 			expect(filter(objects, matches({ 'a': 4, 'c': 6 }))).toEqual(_filter(objects, _matches({ 'a': 4, 'c': 6 })));
// 		});
// 	});
// });
