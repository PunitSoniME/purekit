// import _invertBy from 'lodash/invertBy';
// import { invertBy } from '../../src/object';

// const object = { a: 1, b: 2, c: 1 };

// describe('Object', () => {
// 	describe('invertBy', () => {
// 		it("invertBy(object)", () => {
// 			expect(invertBy(object)).toEqual(_invertBy(object));
// 		});

// 		it("invertBy(object, (value: any) => 'group' + value)", () => {
// 			expect(invertBy(object, (value: any) => 'group' + value)).toEqual(_invertBy(object, (value: any) => 'group' + value));
// 		});
// 	});
// });
