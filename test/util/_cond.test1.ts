// import _cond from 'lodash/cond';
// import { cond } from '../../src/util';
// import { matches } from '../../src/util';

// var func = cond([
// 	[matches({ 'a': 1 }), constant('matches A')],
// 	[conforms({ 'b': _.isNumber }), constant('matches B')],
// ]);

// describe('Util', () => {
// 	describe('cond', () => {
// 		it("cond(predicate, 'string')", () => {
// 			expect(cond(predicate, 'string')).toEqual(_cond(predicate, 'string'));
// 		});
// 	});
// });
