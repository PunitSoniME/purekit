//	@ts-nocheck
const sortBy = <T>(collection: T, iteratee: any[]): T => {
	//	Code needs some modification
	//	return orderBy(collection, iteratee, Array.from(iteratee).fill("asc"));

	return collection;
};

export default sortBy;
