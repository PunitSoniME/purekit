//	@ts-nocheck
const pullAllWith = <T>(
	collection: T[],
	elementsToRemove: T[],
	predicate: any
): T[] => {
	console.warn('pullAllWith - Implementation is still pending');
	return [];
};

export default pullAllWith;
