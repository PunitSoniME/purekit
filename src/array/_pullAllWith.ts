//	@ts-nocheck

/**
 *
 * @param collection
 * @param elementsToRemove
 * @param comparator
 * @returns
 */
const pullAllWith = <T>(
	collection: T[],
	elementsToRemove: T[],
	comparator: Function
): T[] => {
	//	TODO: pullAllWith
	return [];
};

export default pullAllWith;
