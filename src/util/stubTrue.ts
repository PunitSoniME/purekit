/**
 * Returns true.
 *
 * @since 1.0.0
 *
 * @returns {boolean} - The boolean value true.
 *
 * @example
 * const result = stubTrue(); // true
 */
const stubTrue = () => true;
export default stubTrue;
