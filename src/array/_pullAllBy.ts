//	@ts-ignore
const pullAllBy = <T>(
	collection: T[],
	elementsToRemove: T[],
	predicate: any
): T[] => {
	console.warn('pullAllBy - Implementation is still pending');
	return [];
};

export default pullAllBy;
