//	@ts-nocheck
const sortedLastIndexBy = <T>(
	collection: T[],
	value: T,
	predicate: any
): number => {
	console.warn('sortedLastIndex - Implementation is still pending');
	return -1;
};

export default sortedLastIndexBy;
