//	@ts-nocheck

/**
 *
 * @param object
 * @param methodNames
 * @returns
 */
const bindAll = (object: Object, methodNames: string | string[]): any => {
	//	TODO: bindAll
	return true;
};
export default bindAll;
