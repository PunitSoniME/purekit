//	@ts-nocheck

/**
 *
 * @param array
 * @param value
 * @returns
 */
const sortedLastIndex = <T>(array: T[], value: T): number => {
	//	TODO: sortedLastIndex
	return -1;
};

export default sortedLastIndex;
