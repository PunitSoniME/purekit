/**
 * The identity function that returns the value as is.
 *
 * @param {*} value - The value to be returned.
 * @returns {*} The same value as the input.
 */
const identity = <T>(value: T): T => value;
export default identity;
