//	@ts-nocheck

import predicateType from '../helpers/predicateType';
import identity from '../util/identity';

/**
 *
 * @param array
 * @param iteratee
 * @returns
 */
const xorBy = <T>(array: T[], iteratee: predicateType<T> = identity): T[] => {
	//	TODO: xorBy
	return [];
};

export default xorBy;
