/**
 * Creates an empty array.
 *
 * @since 1.0.0
 *
 * @returns {Array} - An empty array.
 *
 * @example
 *
 * const result = stubArray(); // []
 *
 */
const stubArray = () => [];
export default stubArray;
