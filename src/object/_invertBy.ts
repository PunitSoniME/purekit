//	@ts-nocheck

import predicateType from '../helpers/predicateType';

/**
 *
 * @param object
 * @param iteratee
 * @returns
 */
const invertBy = <T>(
	object: Object,
	iteratee: predicateType<T> | null | undefined = null
): Object => {
	return {};
};

export default invertBy;
